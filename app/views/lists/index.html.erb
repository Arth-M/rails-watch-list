<h1>Lists#index</h1>
<p>Find me in app/views/lists/index.html.erb</p>

<%= link_to 'Add a list', new_list_path %>
<% @lists.each do |list| %>
<p><%= list.name  %></p>
<p><%= link_to 'Show me this list', list_path(list) %></p>
<p><%= '*' * 60 %></p>
<% end %>
